import 'dart:convert';
import 'package:flutter_test/flutter_test.dart';

void main() {
  /*
  group('Encryption tests', () {
    test('Valid encryption and decryption', () {
      const String plainText = 'Text';
      final String encrypted = EncryptionProvider.encryption.encrypt(plainText: plainText, password: 'testPassword');
      final String decrypted = EncryptionProvider.encryption.decrypt(encryptedText: encrypted, password: 'testPassword');
      expect(decrypted, plainText);
    });

    test('AES-256 specific', () {
      const String plainText = 'Text';
      final String encrypted = AESEncryption().encrypt(plainText: plainText, password: 'testPassword');
      expect(encrypted, 'vbXzdA==');
    });
  });

  group('Hashing tests', () {
    test('Once SHA-256', () {
      const String input = 'input';
      final String value = Hashing.asString(Hashing.sha256Hash(utf8.encode(input)));
      expect(value, 'c96c6d5be8d08a12e7b5cdc1b207fa6b2430974c86803d8891675e76fd992c20');
    });

    test('Twice SHA-256', () {
      const String input = 'input';
      final String value1 = Hashing.asString(Hashing.sha256Hash(Hashing.sha256Hash(utf8.encode(input))));
      final String value2 = Hashing.asString(Hashing.sha256DoubledHash(utf8.encode(input)));
      expect(value1 == value2, true);
      expect(value1, 'b3c8485039102bd67a91e9ae23c9f8c7b4b7f9907f2866f22ad14f9894d5070f');
    });
  });*/
}